# Agent Instructions

- This repo is the agent workflow integration workspace.
- Frontend and backend are expected to be checked out as git submodules.
- Do not expose source code to PM / UX users in generated answers.
- Use Docker Compose as the default local demo runtime.
- POC branches should be created in submodules or separate worktrees, never directly on main without review.

