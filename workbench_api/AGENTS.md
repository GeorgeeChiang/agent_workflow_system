# Agent Instructions

- This service powers the PM/UX-facing workflow workbench API.
- Store session data in SQLite for simple local persistence.
- Never return source code bodies to PM/UX users; only summaries and file references.
- Repo references should point to mounted `systems/frontend` and `systems/backend` paths when relevant.

