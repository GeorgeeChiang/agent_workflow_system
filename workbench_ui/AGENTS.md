# Agent Instructions

- This repo powers the PM/UX-facing workflow workbench frontend.
- Keep the UI in Traditional Chinese and avoid exposing source code.
- Use the API for all session state instead of keeping durable state only in memory.

